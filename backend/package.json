{
  "name": "cafe-finder-backend",
  "version": "0.1.0",
  "description": "Minimal backend for Cafe Finder: Firebase token verification, admin endpoints, Cloud SQL (Postgres) example",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "migrate": "node run_migrations.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.18.2",
    "firebase-admin": "^11.10.0",
    "pg": "^8.11.0",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
